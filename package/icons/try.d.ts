declare namespace _default {
    export namespace _try {
        const width: number;
        const height: number;
        const paths: {
            d: string;
        }[];
    }
    export { _try as try };
}
export default _default;
